"""auto-experimenter: an autonomous experiment loop for churn models.

The loop plans experiments, runs them with a fixed evaluation protocol, keeps an
append-only ledger, compares the results and narrates a conclusion. See README.md.
"""

__version__ = "0.1.0"
